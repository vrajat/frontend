import { dialects } from "./dialects";
import { features } from "./features";
import { navigation } from "./navigation";
import { version } from "./version";

export { dialects, features, navigation, version };
