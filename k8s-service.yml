kind: Service
apiVersion: v1
metadata:
  name: dblint-ui
  labels:
    app: dblint
    tier: ui
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http-server
  selector:
    app: dblint
    tier: ui
